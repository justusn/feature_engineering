The Jupyter Notebook contains :
 - Reading of the file
 - Dropping unrequired columns
 - Dropping unpopulated columns and replacing null in fields whose column is sufficiently populated.
 - Extracting hour and minute from the different timestamps.
 - Label encoding string values
 - Training the model with all the features.
 - Selecting significant features using PCA and training the model with selected feasutres